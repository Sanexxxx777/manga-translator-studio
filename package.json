{
  "name": "manga-translator-studio",
  "version": "0.1.0",
  "private": true,
  "description": "MangaDex chapter URL → Gemini translation → CBZ. Web UI on top of a Python pipeline built around manga-image-translator.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js"
  },
  "dependencies": {
    "compression": "^1.7.4",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
